//Snake is subclass with no talk() method, hence uses Animal superclass talk() method
package oop7TestAnimalPolymorphsim;
class Snake extends Animal{
	private int legs=0;//only have its own data, will inheritate parent class talk() method
}
